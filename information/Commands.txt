# Start all services
docker-compose up -d

# Stop all services
docker-compose down

# Stop and delete all data (CAREFUL!)
docker-compose down -v

# View logs
docker-compose logs -f [service_name]

# Restart a service
docker-compose restart [service_name]

# Check service health
docker-compose ps

# Access PostgreSQL shell
docker exec -it rag_postgres psql -U postgres -d rag_assistant

# Access Redis shell
docker exec -it rag_redis redis-cli

# Check Qdrant web UI
# Open browser: http://localhost:6333/dashboard

# Delete Qdrant collection of chunks if new files have been added
# Invoke-WebRequest -Uri "http://localhost:6333/collections/chunks" -Method Delete

# See Qdrant collection of chunks
# Invoke-WebRequest -Uri "http://localhost:6333/collections/chunks" -Method Get

# PostgreSQL
docker exec -it rag_postgres psql -U postgres -c "SELECT count(*) FROM file_metadata;"

# Qdrant
curl http://localhost:6333/collections/file_chunks

# Redis
docker exec -it rag_redis redis-cli INFO stats